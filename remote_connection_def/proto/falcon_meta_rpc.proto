syntax = "proto3";

package falcon.meta_proto;

option cc_generic_services = true;

message MetaRequest {
}

message Empty {

}

service MetaService {
    // All request data (including service type) is transferred in FlatBuffer format through attachment.
    rpc MetaCall(MetaRequest) returns(Empty) {}
}
